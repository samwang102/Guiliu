# 为归流贡献

感谢你愿意改进归流。项目优先接受范围明确、可验证，并且不降低文件操作安全性的改动。

## 本地开发

- macOS 14 或更高版本。
- Swift 6.2 或更高版本；推荐使用 Xcode 26 或更高版本。

```bash
swift test
swift run Guiliu
```

## 提交要求

1. 先为行为变化补充或更新测试。
2. 文件移动、重命名、删除、恢复和引用操作必须保留“不覆盖、可校验、可恢复”的安全边界。
3. 不要提交个人文件、真实聊天附件、绝对用户路径、访问令牌、模型权重、构建目录或签名材料。
4. 用户可见行为或技术约束变化需要同步更新公开说明和相关测试。
5. 提交前运行 `swift test`，并在 Pull Request 中写清用户可见变化和验证方式。

## 问题与功能建议

请先搜索现有 Issue。提交新问题时可提供脱敏后的复现步骤、macOS 版本和归流版本，但不要上传含私人内容的原文件。
